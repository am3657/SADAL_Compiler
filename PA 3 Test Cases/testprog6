procedure prog6 is
	-- { Testing Illegal operand type for an arithmetic operation. } 
	
	x, y_1, w234: integer := 2;	
	str : string := "Welcome";
	z, w: float :=  0.25;
	flag , bing : boolean:= false ; 
	
begin
	w := z + w;
	flag := true;
	w := flag * bing;
END prog6;
